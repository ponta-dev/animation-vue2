module.exports = {
  transpileDependencies: [
    'vuetify'
  ],
  // ベースパスの設定
//  publicPath: "/sample/",
  // npm run serve時のサーバーの設定
/*  devServer: {
    // 外部API等を利用する場合のproxy設定
    proxy: {
      '^/context': {
        target: 'http://~~:80',
        changeOrigin: true
      }
    }
  } */
}
